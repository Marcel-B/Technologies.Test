<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:Technology.Mobile.ViewModels;assembly=Technology.Mobile"
             x:Class="Technology.Mobile.MainPage">

    <StackLayout>
        <StackLayout.BindingContext>
            <local:TestViewModel/>
        </StackLayout.BindingContext>

        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" 
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               Margin="4, 0, 4, 0"/>

        <Label Text="{Binding Path=Name}"
               Margin="4, 4, 4, 0"/>

        <Entry Text="{Binding Path=Name}"
               Margin="4, 4, 4, 0"/>

        <Button Command="{Binding CallApi}" 
                Text="Click me"
                Margin="4, 4, 4, 0"/>

        <ListView ItemsSource="{Binding Slipways}"/>
    </StackLayout>
</ContentPage>

